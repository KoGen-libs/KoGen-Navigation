project:
  name: KoGenNavigation
  description: A compose navigation library for Kotlin
  authors:
    - Eugen Kopp
  license: Apache-2.0
  inceptionYear: '2025'
  languages:
    java:
      groupId: 'io.github.eugenprog'

release:
  github:
    branch: 'master'

# Настройки GPG-подписи
signing:
  active: ALWAYS
  pgp:
    armored: true

# Настройки загрузки в Maven Central
deploy:
  maven:
    mavenCentral:
      sonatype:
        active: 'ALWAYS'
        stagingRepositories:
          - 'koGenNavigationCommon/build/staging-deploy'
          - 'koGenNavigation/build/staging-deploy'
          - 'koGenNavigationCompiler/build/staging-deploy'
        url: 'https://central.sonatype.com/api/v1/publisher'
        namespace: 'io.github.eugenprog'
        # PomChecker (внутренняя проверка JReleaser) не понимает packaging=aar
        # у Android-библиотек и падает на этапе валидации. Центральный портал
        # Sonatype это принимает нормально, поэтому отключаем локальную проверку.
        # https://github.com/jreleaser/jreleaser.github.io/issues/85
        verifyPom: false